function cambioPantallas (){

 if ( estado === 1 ) {
    mostrar( 1 , dialogos );
}
  else if ( estado === 2 ) {
    mostrar( 2 , dialogos , [["Regresar", 530, 10 , 110 , 30]] );
} 
  else if ( estado === 3 ) {
    mostrar( 3 , dialogos );
} 
  else if ( estado === 4 ) {
    mostrar( 4 , dialogos );
} 
  else if ( estado === 5 ) {
    mostrar( 5 , dialogos , [["seguir al hachero", 420, 240 , 200 , 50] , ["cambiar de direccion", 420, 300 , 200 , 50]]);
} 
  else if ( estado === 6 ) {
    mostrar( 6 , dialogos , [["seguir por su cuenta", 420, 240 , 200 , 50] , ["seguir la voz", 420, 300 , 200 , 50]]);
} 
  else if ( estado === 7 ) {
    mostrar( 7 , dialogos , [["aceptar", 420, 240 , 200 , 50] , ["pensarlo", 420, 300 , 200 , 50]] );
} 
  else if ( estado === 8 ) {
    mostrar( 8 , dialogos );
} 
  else if ( estado === 9 ) {
    mostrar( 9 , dialogos );
} 
  else if ( estado === 10 ) {
    mostrar( 10 , dialogos , [["Marcharse", 420, 240 , 200 , 50] , ["quedarse por ahora", 420, 300 , 200 , 50]] );
} 
  else if ( estado === 11 ) {
    mostrar( 11 , dialogos );
} 
  else if ( estado === 12 ) {
    mostrar( 12 , dialogos );
} 
  else if ( estado === 13 ) {
    mostrar( 13 , dialogos , [["Lanzar y huir", 420, 240 , 200 , 50] , ["Huir", 420, 300 , 200 , 50]] );
} 
  else if ( estado === 14 ) {
    mostrar( 14 , dialogos );
} 
  else if ( estado === 15 ) {
    mostrar( 15 , dialogos );
} 
  else if ( estado === 16 ) {
    mostrar( 16 , dialogos , [["Lanzar", 420, 240 , 200 , 50] , ["Lanzar", 420, 300 , 200 , 50]] );
} 
  else if ( estado === 17 ) {
    mostrar( 17 , dialogos );
} 
  else if ( estado === 18 ) {
    mostrar( 18 , dialogos );
} 
  else if ( estado === 19 ) {
    mostrar( 19 , dialogos );
} 
  else if ( estado === 20 ) {
    mostrar( 20 , dialogos );
} 
  else if ( estado === 21 ) {
    mostrar( 21 , dialogos );
} 
  else if ( estado === 22 ) {
    mostrar( 22, dialogos , [["aceptar", 420, 240 , 200 , 50] , ["rechazar", 420, 300 , 200 , 50]] );
} 

}//--------------------------------------------------------------------------corchete de cierre del CAMBIO DE PANTALLA
